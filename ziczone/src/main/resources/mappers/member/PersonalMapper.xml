<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.ziczone.mapper.member.PersonalUserMapper">
    <select id="findByUser_UserId" parameterType="long" resultType="org.zerock.ziczone.domain.member.PersonalUser">
        SELECT * FROM personal_user WHERE user_id = #{userId}
    </select>
</mapper>