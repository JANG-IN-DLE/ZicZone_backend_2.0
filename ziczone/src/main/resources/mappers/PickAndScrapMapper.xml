<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper>
    <select id="findByPersonalUser" parameterType="long" resultType="org.zerock.ziczone.mapper.PickAndScrapMapper">
        SELECT * FROM pick_and_scrap WHERE personal_id = #{personalId}
    </select>
</mapper>