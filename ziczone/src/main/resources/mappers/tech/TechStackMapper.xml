<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.ziczone.mapper.tech.TechStackMapper">
    <select id="findByPersonalUserPersonalId" parameterType="long" resultType="org.zerock.ziczone.domain.tech.TechStack">
        SELECT * FROM tech_stack WHERE personal_id = #{personalId}
    </select>
</mapper>